// boolean indicating whether the user has started the game or not
var gameStarted = false;

// boolean indicating whether the page has changed color or not
var colorChanged = false;

// variable to store the timeout ID for changing the page color
var timeoutId = null;

// variable to store the time when the page color changed
var colorChangeTime = null;
